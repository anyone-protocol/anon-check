name: STAGE - Build and Push

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out the repo
        uses: actions/checkout@v4
      - name: Build and push
        uses: ./.github/actions/build-and-push
        with:
          docker-username: ${{ github.actor }}
          docker-password: ${{ secrets.GITHUB_TOKEN }}
          docker-tag: ${{ github.sha }}
      # Deploy is manual, from a checkout of the built commit:
      #   sed 's/DEPLOY_TAG/<sha>/g' operations/deploy-stage.hcl > /tmp/stage.hcl
      #   nomad job run /tmp/stage.hcl
      # - name: Deploy
      #   uses: ./.github/actions/deploy
      #   with:
      #     environment: stage
      #     nomad-cacert: operations/admin-ui-ca.crt
      #     nomad-token: ${{ secrets.NOMAD_TOKEN_CHECK_DEPLOY }}
      #     nomad-addr: ${{ secrets.NOMAD_ADDR }}
      #     docker-tag: ${{ github.sha }}
